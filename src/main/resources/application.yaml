spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/riyatuitionapp
    username: root
    password: vaishu@21

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 7070
  servlet:
    context-path: /api

session:
  timeout: 300000
